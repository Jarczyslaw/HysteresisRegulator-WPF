<Application x:Class="HysteresisRegulator.App" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:HysteresisRegulator"
             xmlns:core="clr-namespace:System;assembly=mscorlib"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
             xmlns:d1p1="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d1p1:Ignorable="d" 
             StartupUri="/Views/MainView.xaml">
    <Application.Resources>
        
        <core:Double x:Key="WindowHeight">700</core:Double>
        <core:Double x:Key="WindowWidth">1000</core:Double>

        <Thickness x:Key="defaultTopMargin" Left="0" Top="5" Right="0" Bottom="0" />

        <Style TargetType="GroupBox" x:Key="boldGroupBox">
            <Setter Property="Padding" Value="3" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontWeight="Bold" />
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>